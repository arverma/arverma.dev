// @ts-check
import { defineConfig } from "astro/config";

// Project Pages URL: https://arverma.github.io/arverma.dev/
// When you switch to a root custom domain (arverma.dev), set base to "/" and site to that domain.
export default defineConfig({
  site: "https://arverma.github.io",
  base: "/arverma.dev",
});
